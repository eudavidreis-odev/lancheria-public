/**
 * @packageDocumentation
 * Re-export do hook `useColorScheme` do React Native. Mantido separado para poder
 * trocar implementações por plataforma (ex.: `use-color-scheme.web.ts`) e para
 * ser documentado pelo TypeDoc.
 */
export { useColorScheme } from 'react-native';
